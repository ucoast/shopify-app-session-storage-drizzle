/** @type {import("prettier").Config} */
const config = {
  arrowParens: "always",
  printWidth: 80,
  singleQuote: true,
  semi: false,
  trailingComma: "all",
  tabWidth: 4,
  useTabs: true,
};

module.exports = config;
